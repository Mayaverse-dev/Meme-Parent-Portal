{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm install && npm run build && cp -r dist/* ../static/ && cd .. && pip install -r requirements.txt"
  }
}
